server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost/supportportal
    password:
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect